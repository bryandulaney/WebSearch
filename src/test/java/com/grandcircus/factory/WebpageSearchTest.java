package com.grandcircus.factory;

import org.junit.Test;


public class WebpageSearchTest {

	@Test
	public void propertyInstantiation() {
		
/*		// There will be 3 properties instantiated:
		// (1) String called mWebAddress
		boolean a = WebpageSearch.mWebAddress instanceof String;

		// (2) String called mWebPage
		boolean b = WebpageSearch.mWebPage instanceof String;
		
		// (3) ArrayList called mPageIndex
		boolean c = WebpageSearch.mPageIndex instanceof ArrayList;	
		
		
		if (!( a && b && c)) {
			fail("Not yet implemented");			
		}
*/

	}

	@Test
	public void methodInstantiation() {
		
/*		// There will be 7 methods including one constructor

		// nameFile method will create a name for the new HTML file as a string using the URL
		String inputAddress = "https://medium.com/five-hundred-words/spring-is-coming-c617275a3d76";
		String outputName = "spring-is-coming-c617275a3d76.html";
		
		boolean nameFile = WebpageSearch.nameFile(inputAddress).equals(outputName);
		
		if (!nameFile) {
			fail("Not the same.");
		}
		
		// inputStream method
		
		*/
	}
	
}
